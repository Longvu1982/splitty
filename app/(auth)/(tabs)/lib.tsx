import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const lib = () => {
  return (
    <View>
      <Text>lib</Text>
    </View>
  );
};

export default lib;

const styles = StyleSheet.create({});
